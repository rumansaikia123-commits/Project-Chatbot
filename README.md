# Project Chatbot

A chatbot project built while learning to create AI applications from scratch.

## What this project does

(To be filled in as the project takes shape.)

## Setup

1. Copy `.env.example` to a new file named `.env`
2. Fill in your real API key inside `.env`
3. (Run instructions will be added here once the project has code to run.)

## Notes

- `.env` holds secret keys and is never uploaded to GitHub (see `.gitignore`).
- `.env.example` shows what keys are needed, without revealing real values.
